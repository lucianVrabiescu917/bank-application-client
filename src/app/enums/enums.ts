export enum Status {
	ACCEPTED = 'ACCEPTED',
	DECLINED = 'DECLINED',
	DECLINED_BANK = 'DECLINED BANK',
	ACCEPTED_BANK = 'ACCEPTED BANK',
	PENDING = 'PENDING'
}

export enum UnitEnum {
	KGS = 'kgs',
	LBS = 'lbs',
	TONS = 'tons',
	LITERS = 'liters'
}

export enum RoleEnum {
	IMPORTER = 'IMPORTER',
	EXPORTER = 'EXPORTER',
	BANK = 'BANK',
}

